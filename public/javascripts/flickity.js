console.log("flickity js loaded");

var bookflkty = new Flickity( '.books-gallery', {
  // options
  cellAlign: 'left',
  contain: true
});

var foodflkty = new Flickity( '.foods-gallery', {
  // options
  cellAlign: 'left',
  contain: true
});


var albumflkty = new Flickity( '.albums-gallery', {
  // options
  cellAlign: 'left',
  contain: true
});
